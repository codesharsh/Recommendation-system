# Recommendation-system
Movie Recommendation System — An ML project comparing popularity-based, collaborative filtering, matrix factorization (SVD), content-based, and hybrid recommenders on the MovieLens dataset, with honest benchmarking (SVD wins; KNN and hybrid underperformed in specific measurable ways) and time-based evaluation to avoid data leakage.
